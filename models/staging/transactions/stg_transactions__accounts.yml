version: 2

models:
  - name: stg_transactions__accounts
    columns:
      - name: account_sk
        tests:
          - not_null
          - unique

  - name: customer_id
    tests:
    - relationships:
        to: ref('stg_crm__customers')
        field: customer_id